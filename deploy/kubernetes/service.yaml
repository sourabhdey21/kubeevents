apiVersion: v1
kind: Service
metadata:
  name: kubeevents
  namespace: kubeevents
  labels:
    app.kubernetes.io/name: kubeevents
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: kubeevents
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
